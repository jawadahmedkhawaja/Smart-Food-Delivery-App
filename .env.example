# Firebase Configuration
# Note: These are examples. Get your actual values from Firebase Console

# Google Maps API Key
# Get this from: https://console.cloud.google.com/
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Firebase Project ID
FIREBASE_PROJECT_ID=your_firebase_project_id

# Notes:
# 1. Copy this file to `.env` and fill in your actual values
# 2. Never commit the actual `.env` file to version control
# 3. The Google Maps API key in AndroidManifest.xml will be removed after project submission
# 4. For Firebase credentials, use the google-services.json file instead
